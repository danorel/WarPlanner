package com.danorel.warplanner.agents.crystal;

import com.danorel.warplanner.agents.base.BaseAgent;

public class CrystalAgent extends BaseAgent {
    public CrystalAgent(String asset, float initialX, float initialY) {
        super(asset, initialX, initialY);
    }
}
